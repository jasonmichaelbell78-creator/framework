---
phase: 01-foundation
plan: 04
type: execute
wave: 2
depends_on: ['01-01']
files_modified:
  - CANON/standards/JSONL_STANDARD.md
  - CANON/standards/DEPENDENCY_STANDARD.md
  - CANON/standards/INTERACTION_STANDARD.md
autonomous: true

must_haves:
  truths:
    - 'JSONL_STANDARD defines universal base record format and JSONL patterns'
    - 'DEPENDENCY_STANDARD defines the 7 relationship types and registry format'
    - 'INTERACTION_STANDARD defines UX patterns for skills and interactive workflows'
    - 'Each standard has machine-checkable enforcement defined'
  artifacts:
    - path: 'CANON/standards/JSONL_STANDARD.md'
      provides: 'JSONL file format, base record fields, rotation, dual-write patterns'
      contains: 'base-record.schema'
    - path: 'CANON/standards/DEPENDENCY_STANDARD.md'
      provides: 'Dependency registry format, 7 relationship types, auto-discovery'
      contains: 'DEPENDENCY_GRAPH.jsonl'
    - path: 'CANON/standards/INTERACTION_STANDARD.md'
      provides: 'Interactive skill UX patterns, confirmation flows, output formatting'
      contains: 'checkpoint'
  key_links:
    - from: 'CANON/standards/JSONL_STANDARD.md'
      to: 'CANON/schemas/base-record.schema.ts'
      via: 'references base record schema'
      pattern: "base-record\\.schema"
    - from: 'CANON/standards/DEPENDENCY_STANDARD.md'
      to: 'scripts/trace-dependencies.js'
      via: 'references auto-discovery script'
      pattern: 'trace-dependencies'
---

<objective>
Write the remaining 3 new CANON standards: JSONL_STANDARD, DEPENDENCY_STANDARD, and INTERACTION_STANDARD. These three inform downstream work (dependency registry format, JSONL patterns used by resource survey).

Purpose: JSONL_STANDARD and DEPENDENCY_STANDARD define the data formats used by the dependency registry and resource survey. INTERACTION_STANDARD completes the standards set.
Output: 3 standards documents in CANON/standards/, completing the full set of 10 standards (SKILL_STANDARDS migrated in Plan 02, AUDIT_STANDARD created in Plan 02, 5 written in Plan 03, 3 written here).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@CLAUDE.md
@scripts/config/audit-schema.json
@scripts/trace-dependencies.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write JSONL_STANDARD and DEPENDENCY_STANDARD</name>
  <files>CANON/standards/JSONL_STANDARD.md, CANON/standards/DEPENDENCY_STANDARD.md</files>
  <action>
    **JSONL_STANDARD.md** (~120-160 lines):
    - Read `scripts/config/audit-schema.json` for existing field definitions
    - Read `CANON/schemas/base-record.schema.ts` (created in Plan 01) for base fields
    - Document the JSONL-as-source-of-truth pattern: JSONL is canonical, MD views are generated
    - Document base record requirements: every JSONL record MUST include `id`, `timestamp`, `version`
    - Document the dual-write pattern (MASTER_DEBT + deduped.jsonl pattern)
    - Document rotation pattern: reference `rotate-state.js` for archive-on-rotation
    - Document safe reading: reference `scripts/lib/read-jsonl.js` for blank line handling, malformed JSON
    - Document safe writing: atomic writes via `safe-fs.js`, never append without locking
    - Enforcement: Zod schema validation at read boundaries (base-record.schema.ts)

    **DEPENDENCY_STANDARD.md** (~120-160 lines):
    - Define the DEPENDENCY_GRAPH.jsonl format:
      - Each record: `{ id, timestamp, version, source, target, type, confidence, metadata }`
      - 7 relationship types: `requires`, `invokes`, `references`, `triggers`, `generates`, `validates`, `extends`
      - Confidence levels: `high` (auto-discovered code deps), `medium` (auto-discovered non-code), `low` (manual entry)
    - Document auto-discovery via `scripts/trace-dependencies.js` (scans require/import)
    - Document the registry population flow: auto-discover -> review non-code -> persist
    - Document the two-tier reminder system: session-end nudge + phase gate check
    - Document system-level vs file-level granularity (system edges hand-curated, file edges auto)
    - Enforcement: staleness check at phase boundaries + `/review-deps` skill validation

  </action>
  <verify>
    - Each file has all 6 required sections
    - JSONL_STANDARD references base-record.schema.ts
    - DEPENDENCY_STANDARD defines all 7 relationship types
    - `grep -c "MUST\|SHOULD\|MAY" CANON/standards/JSONL_STANDARD.md` returns > 5
    - `grep -c "MUST\|SHOULD\|MAY" CANON/standards/DEPENDENCY_STANDARD.md` returns > 5
  </verify>
  <done>JSONL_STANDARD and DEPENDENCY_STANDARD written with data format definitions, enforcement, and cross-references to existing tooling</done>
</task>

<task type="auto">
  <name>Task 2: Write INTERACTION_STANDARD</name>
  <files>CANON/standards/INTERACTION_STANDARD.md</files>
  <action>
    **INTERACTION_STANDARD.md** (~100-140 lines):
    - Read existing skill patterns for UX conventions (SKILL_STANDARDS in CANON already covers some)
    - Document interactive flow patterns:
      - Confirmation before destructive operations (MUST)
      - Progress indicators for long operations (SHOULD)
      - Checkpoint pattern: present -> confirm -> execute -> report
      - Error recovery: offer retry/skip/abort on failure
    - Document output formatting:
      - Structured output with headers and sections
      - Summary at top, details below
      - File paths always absolute or repo-relative
      - Tables for multi-item comparisons
    - Document skill invocation patterns:
      - `/skill-name` syntax
      - Parameter passing conventions
      - Return value expectations (success message, error with actionable next steps)
    - Document agent interaction patterns:
      - Spawn protocol, input format, return protocol
      - Reference AGENT_STANDARD for return format
    - Enforcement: skill validation script checks for required interaction patterns
  </action>
  <verify>
    - File has all 6 required sections
    - `grep -c "MUST\|SHOULD\|MAY" CANON/standards/INTERACTION_STANDARD.md` returns > 3
    - Standard covers confirmation, progress, checkpoints, error recovery, output formatting
  </verify>
  <done>INTERACTION_STANDARD written, completing all 10 CANON standards</done>
</task>

</tasks>

<verification>
- 3 new standard files exist in CANON/standards/
- After Plans 02, 03, and 04 complete: total of 10 standards in CANON/standards/
  (SKILL_STANDARDS, AUDIT_STANDARD, AGENT_STANDARD, HOOK_STANDARD, DOC_STANDARD,
   NAMING_CONVENTIONS, RECOVERY_STANDARD, JSONL_STANDARD, DEPENDENCY_STANDARD, INTERACTION_STANDARD)
- All follow canonical structure with RFC 2119 keywords
- JSONL_STANDARD cross-references base-record.schema.ts
- DEPENDENCY_STANDARD defines all 7 relationship types
</verification>

<success_criteria>

- `ls CANON/standards/*.md | wc -l` shows 10 standards files (after Plans 02+03+04 all complete)
- JSONL_STANDARD defines base record format referencing the Zod schema
- DEPENDENCY_STANDARD defines DEPENDENCY_GRAPH.jsonl record structure with 7 types
- INTERACTION_STANDARD covers interactive workflow patterns
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
