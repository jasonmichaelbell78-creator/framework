---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ['01-01', '01-02']
files_modified:
  - CANON/standards/AGENT_STANDARD.md
  - CANON/standards/HOOK_STANDARD.md
  - CANON/standards/DOC_STANDARD.md
  - CANON/standards/NAMING_CONVENTIONS.md
  - CANON/standards/RECOVERY_STANDARD.md
autonomous: true

must_haves:
  truths:
    - 'Each standard follows the canonical structure: Purpose, Scope, Requirements, Enforcement, Escape Hatches, Version History'
    - 'Standards clearly distinguish mandatory vs optional requirements'
    - 'Each standard has a machine-checkable enforcement mechanism defined'
    - 'Standards accurately reflect existing codebase patterns'
  artifacts:
    - path: 'CANON/standards/AGENT_STANDARD.md'
      provides: 'Agent file structure, naming, frontmatter, return protocol'
      contains: 'COMPLETE:'
    - path: 'CANON/standards/HOOK_STANDARD.md'
      provides: 'Hook event types, registration, shared libraries, error handling'
      contains: 'SessionStart'
    - path: 'CANON/standards/DOC_STANDARD.md'
      provides: 'Document header requirements, categories, naming'
      contains: 'doc-header-config'
    - path: 'CANON/standards/NAMING_CONVENTIONS.md'
      provides: 'Naming patterns for all file types'
      contains: 'kebab-case'
    - path: 'CANON/standards/RECOVERY_STANDARD.md'
      provides: 'Recovery skill patterns, state restoration, error recovery'
      contains: 'recovery'
  key_links:
    - from: 'CANON/standards/HOOK_STANDARD.md'
      to: '.claude/settings.json'
      via: 'references hook registration'
      pattern: "settings\\.json"
    - from: 'CANON/standards/DOC_STANDARD.md'
      to: 'scripts/config/doc-header-config.json'
      via: 'references doc conventions'
      pattern: 'doc-header-config'
---

<objective>
Write 5 of the 8 new CANON standards: AGENT_STANDARD, HOOK_STANDARD, DOC_STANDARD, NAMING_CONVENTIONS, and RECOVERY_STANDARD. Each standard formalizes existing codebase patterns into the canonical document structure.

Purpose: These 5 standards cover the most independent domains and can be written by examining existing code patterns without depending on each other.
Output: 5 standards documents in CANON/standards/, each with enforcement mechanisms defined.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@CLAUDE.md
@.claude/settings.json
@scripts/config/doc-header-config.json

Note: This project runs on Windows with bash shell available. Verify commands use Unix-style
syntax which works in the bash shell configured for this environment.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write AGENT_STANDARD and HOOK_STANDARD</name>
  <files>CANON/standards/AGENT_STANDARD.md, CANON/standards/HOOK_STANDARD.md</files>
  <action>
    For each standard, follow the canonical structure from RESEARCH.md:
    Purpose, Scope, Requirements (RFC 2119 MUST/SHOULD/MAY), Enforcement, Escape Hatches, Version History.

    **AGENT_STANDARD.md** (~100-150 lines):
    - Read existing agents in `.claude/agents/` to extract current patterns
    - Document: YAML frontmatter requirements (name, description), file naming (lowercase-kebab-case.md), directory placement
    - Document return protocol: `COMPLETE: [agent-id] wrote N findings to [output-path]`
    - Document companion file pattern (REFERENCE.md, prompts.md)
    - Size limits: <300 core lines, 300-499 warning, 500+ error
    - Enforcement: validation script (to be built in Phase 4, document the mechanism now)

    **HOOK_STANDARD.md** (~150-200 lines):
    - Read `.claude/settings.json` for hook registration patterns and `.claude/hooks/` for implementation patterns
    - Document all 5 event types: SessionStart, PreToolUse, PostToolUse, PreCompact, UserPromptSubmit
    - Document registration in settings.json (command, bash type, timeout patterns)
    - Document shared library usage from `.claude/hooks/lib/` (6 modules)
    - Document error handling: hooks must not crash Claude sessions, use try/catch
    - Document skip mechanism: SKIP_CHECKS + SKIP_REASON environment variable pattern
    - Enforcement: hook linting (ESLint rules + validation script)

  </action>
  <verify>
    - Each file contains all 6 required sections: Purpose, Scope, Requirements, Enforcement, Escape Hatches, Version History
    - `grep -c "MUST\|SHOULD\|MAY" CANON/standards/AGENT_STANDARD.md` returns > 5
    - `grep -c "MUST\|SHOULD\|MAY" CANON/standards/HOOK_STANDARD.md` returns > 5
  </verify>
  <done>Two standards written with complete structure, RFC 2119 requirements, and enforcement mechanisms</done>
</task>

<task type="auto">
  <name>Task 2: Write DOC_STANDARD, NAMING_CONVENTIONS, and RECOVERY_STANDARD</name>
  <files>CANON/standards/DOC_STANDARD.md, CANON/standards/NAMING_CONVENTIONS.md, CANON/standards/RECOVERY_STANDARD.md</files>
  <action>
    For each standard, follow the canonical structure from RESEARCH.md:
    Purpose, Scope, Requirements (RFC 2119 MUST/SHOULD/MAY), Enforcement, Escape Hatches, Version History.

    **DOC_STANDARD.md** (~100-120 lines):
    - Read `scripts/config/doc-header-config.json` for existing header conventions
    - Document required headers per document category (canonical, foundation, planning, reference, guide)
    - Document file naming: UPPERCASE_SNAKE_CASE for standards, lowercase-kebab-case for guides
    - Document version history table requirement
    - Enforcement: `scripts/check-doc-headers.js` (existing)

    **NAMING_CONVENTIONS.md** (~80-120 lines):
    - Extract patterns documented in RESEARCH.md "Existing naming patterns" section:
      - Skills: lowercase-kebab-case directories
      - Agents: lowercase-kebab-case .md files
      - Hooks: lowercase-kebab-case .js files
      - Hook libs: lowercase-kebab-case .js files
      - Scripts: lowercase-kebab-case .js files
      - Config files: lowercase-kebab-case .json files
      - Standards: UPPERCASE_SNAKE_CASE .md files
      - Templates: UPPERCASE with .template.md suffix
    - Document JSONL file naming conventions (UPPERCASE_SNAKE_CASE.jsonl)
    - Document directory naming (lowercase-kebab-case)
    - Document environment variable naming (UPPER_SNAKE_CASE)
    - Enforcement: ESLint custom rule (can be added to eslint-plugin-framework) + naming check script

    **RECOVERY_STANDARD.md** (~80-120 lines):
    - Read existing recovery patterns from skills and hooks
    - Document: state persistence patterns (save before risky operations)
    - Document: compaction resilience (save state, restore after compact)
    - Document: orphaned state detection and cleanup
    - Document: lockfile patterns for concurrent access
    - Reference `scripts/lib/safe-fs.js` for atomic write patterns
    - Reference `.claude/hooks/lib/rotate-state.js` for state rotation
    - Enforcement: code review checklist + recovery skill validation

  </action>
  <verify>
    - Each file contains all 6 required sections
    - `grep -c "MUST\|SHOULD\|MAY" CANON/standards/DOC_STANDARD.md` returns > 5
    - `grep -c "MUST\|SHOULD\|MAY" CANON/standards/NAMING_CONVENTIONS.md` returns > 3
    - `grep -c "MUST\|SHOULD\|MAY" CANON/standards/RECOVERY_STANDARD.md` returns > 3
    - NAMING_CONVENTIONS covers all 8+ file type patterns
  </verify>
  <done>Three standards written covering doc conventions, naming patterns, and recovery procedures, all with enforcement defined</done>
</task>

</tasks>

<verification>
- 5 new standard files exist in CANON/standards/
- All follow the canonical document structure
- All use RFC 2119 keywords (MUST/SHOULD/MAY) to clearly distinguish mandatory vs optional requirements
- All have enforcement mechanisms documented
- Content accurately reflects existing codebase patterns (not aspirational)
</verification>

<success_criteria>

- `ls CANON/standards/*.md | wc -l` shows at least 7 (5 new + 1 migrated SKILL_STANDARDS + 1 new AUDIT_STANDARD from Plan 02)
- Each standard has Purpose, Scope, Requirements, Enforcement, Escape Hatches, Version History sections
- Standards reference real existing files/patterns in the codebase
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
