---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CANON/standards/SKILL_STANDARDS.md
  - CANON/standards/AUDIT_STANDARD.md
  - CANON/templates/AUDIT_TEMPLATE.md
autonomous: true

must_haves:
  truths:
    - 'SKILL_STANDARDS.md lives in CANON/standards/ as the single source of truth'
    - 'AUDIT_TEMPLATE.md lives in CANON/templates/ as the single source of truth'
    - 'AUDIT_STANDARD.md exists in CANON/standards/ defining audit methodology'
    - 'No duplicate copies exist in .claude/skills/_shared/'
    - 'All references in skills point to CANON locations'
  artifacts:
    - path: 'CANON/standards/SKILL_STANDARDS.md'
      provides: 'Skill development standards'
      min_lines: 250
    - path: 'CANON/standards/AUDIT_STANDARD.md'
      provides: 'Audit methodology standard: taxonomy, required components, scoring'
      min_lines: 80
    - path: 'CANON/templates/AUDIT_TEMPLATE.md'
      provides: 'Audit output template (boilerplate for audit reports)'
      min_lines: 180
  key_links:
    - from: '.claude/skills/**/index.md'
      to: 'CANON/standards/SKILL_STANDARDS.md'
      via: '@ file reference'
      pattern: 'CANON/standards/SKILL_STANDARDS'
    - from: 'CANON/standards/AUDIT_STANDARD.md'
      to: 'CANON/templates/AUDIT_TEMPLATE.md'
      via: 'references audit template for report output'
      pattern: 'AUDIT_TEMPLATE'
---

<objective>
Migrate SKILL_STANDARDS.md and AUDIT_TEMPLATE.md from .claude/skills/_shared/ to CANON, create the new AUDIT_STANDARD.md (the 10th standard, defining audit methodology separate from the template), update all references across the codebase, and remove the originals. This eliminates dual-source-of-truth before new standards are written.

Purpose: Standards migration must happen before writing new standards so all standards live in one place from the start. AUDIT_STANDARD.md is distinct from AUDIT_TEMPLATE.md: the standard defines audit methodology, required components, and scoring; the template provides boilerplate for audit report output.
Output: Two migrated files in CANON, one new AUDIT_STANDARD.md, zero references to old \_shared/ locations.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.claude/skills/_shared/SKILL_STANDARDS.md
@.claude/skills/_shared/AUDIT_TEMPLATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Standards Files to CANON and Create AUDIT_STANDARD</name>
  <files>CANON/standards/SKILL_STANDARDS.md, CANON/standards/AUDIT_STANDARD.md, CANON/templates/AUDIT_TEMPLATE.md</files>
  <action>
    1. Copy `.claude/skills/_shared/SKILL_STANDARDS.md` to `CANON/standards/SKILL_STANDARDS.md`
       - Update the document header to include standard metadata format:
         - Add `**Document Version:** 1.0`, `**Last Updated:** 2026-03-02`, `**Status:** ACTIVE`
         - Keep all existing content intact -- this is a migration, not a rewrite
       - Add an `## Enforcement` section at the bottom referencing `scripts/validate-skills.js` as the enforcement mechanism
       - Add an `## Escape Hatches` section documenting how to override (justified deviation with comment explaining why)

    2. Copy `.claude/skills/_shared/AUDIT_TEMPLATE.md` to `CANON/templates/AUDIT_TEMPLATE.md`
       - Add standard header metadata: `**Document Version:** 1.0`, `**Last Updated:** 2026-03-02`, `**Status:** ACTIVE`
       - Keep all existing content intact

    3. Create `CANON/standards/AUDIT_STANDARD.md` as a NEW standard document (~80-120 lines).
       This is the 10th CANON standard, distinct from the template. It defines the audit methodology itself:
       - Follow the canonical structure: Purpose, Scope, Requirements, Enforcement, Escape Hatches, Version History
       - **Purpose**: Define the framework's audit methodology for system and ecosystem audits
       - **Scope**: All audits run by audit agents and orchestrators
       - **Requirements** (use RFC 2119 keywords MUST/SHOULD/MAY):
         - Audit taxonomy: define audit categories (ecosystem, security, compliance, standards-adherence)
         - Required components every audit MUST produce: summary, findings list, severity classification, recommendations
         - Severity levels: S0 (blocker), S1 (high), S2 (medium), S3 (low), S4 (info)
         - Scoring methodology: how to compute pass/fail and quality scores
         - JSONL output: audit results MUST be persisted as JSONL records following base-record schema
         - MD report: audit results SHOULD generate a human-readable markdown report using AUDIT_TEMPLATE.md
       - **Enforcement**: audit orchestrator validates output against this standard
       - **Escape Hatches**: partial audits acceptable when scoped and documented
       - Reference `CANON/templates/AUDIT_TEMPLATE.md` as the report output template

    4. Do NOT delete the originals yet -- Task 2 handles that after references are updated.

  </action>
  <verify>
    - `diff .claude/skills/_shared/SKILL_STANDARDS.md CANON/standards/SKILL_STANDARDS.md` shows only header additions
    - `diff .claude/skills/_shared/AUDIT_TEMPLATE.md CANON/templates/AUDIT_TEMPLATE.md` shows only header additions
    - Both CANON files exist and have content
    - `CANON/standards/AUDIT_STANDARD.md` exists with all 6 required sections
    - `grep -c "MUST\|SHOULD\|MAY" CANON/standards/AUDIT_STANDARD.md` returns > 3
  </verify>
  <done>Both files migrated to CANON with standard headers added, AUDIT_STANDARD.md created as 10th standard, originals still in place</done>
</task>

<task type="auto">
  <name>Task 2: Update All References and Remove Originals</name>
  <files>.claude/skills/**/*.md</files>
  <action>
    1. Find ALL files referencing the old paths:
       - Search for `.claude/skills/_shared/SKILL_STANDARDS.md` across the entire repo
       - Search for `.claude/skills/_shared/AUDIT_TEMPLATE.md` across the entire repo
       - Search for `_shared/SKILL_STANDARDS` (partial references)
       - Search for `_shared/AUDIT_TEMPLATE` (partial references)

    2. Update each reference to point to the CANON location:
       - `.claude/skills/_shared/SKILL_STANDARDS.md` -> `CANON/standards/SKILL_STANDARDS.md`
       - `.claude/skills/_shared/AUDIT_TEMPLATE.md` -> `CANON/templates/AUDIT_TEMPLATE.md`
       - Handle both `@` prefixed references (Claude context references) and plain path references

    3. After ALL references are updated, delete the originals:
       - Remove `.claude/skills/_shared/SKILL_STANDARDS.md`
       - Remove `.claude/skills/_shared/AUDIT_TEMPLATE.md`
       - If `.claude/skills/_shared/` is now empty, remove the directory
       - If it still has other files, leave it

    4. Verify no orphaned references remain:
       - Search entire repo for `_shared/SKILL_STANDARDS` -- must return zero results
       - Search entire repo for `_shared/AUDIT_TEMPLATE` -- must return zero results

  </action>
  <verify>
    - `grep -r "_shared/SKILL_STANDARDS" . --include="*.md" | wc -l` returns 0
    - `grep -r "_shared/AUDIT_TEMPLATE" . --include="*.md" | wc -l` returns 0
    - `ls .claude/skills/_shared/SKILL_STANDARDS.md 2>/dev/null` returns error (file removed)
    - `ls .claude/skills/_shared/AUDIT_TEMPLATE.md 2>/dev/null` returns error (file removed)
    - `ls CANON/standards/SKILL_STANDARDS.md CANON/standards/AUDIT_STANDARD.md CANON/templates/AUDIT_TEMPLATE.md` shows all three exist
  </verify>
  <done>
    - Zero references to old _shared/ locations remain in the codebase
    - Original files deleted
    - CANON is the single source of truth for all three files
  </done>
</task>

</tasks>

<verification>
- CANON/standards/SKILL_STANDARDS.md exists with standard header and original content
- CANON/standards/AUDIT_STANDARD.md exists with all 6 canonical sections and RFC 2119 keywords
- CANON/templates/AUDIT_TEMPLATE.md exists with standard header and original content
- Zero grep hits for `_shared/SKILL_STANDARDS` or `_shared/AUDIT_TEMPLATE` in .md files
- No files remain in _shared/ that duplicate CANON content
</verification>

<success_criteria>

- Single source of truth: migrated files exist only in CANON
- AUDIT_STANDARD.md is a proper standard (not a template) defining audit methodology
- All skill files reference CANON paths
- Content preserved from originals (migration, not rewrite)
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
