---
phase: 01-foundation
plan: 07
type: execute
wave: 2
depends_on: ['01-01']
files_modified:
  - CANON/RESOURCE_SURVEY.jsonl
  - CANON/RESOURCE_SURVEY.md
autonomous: false

must_haves:
  truths:
    - 'Resource survey covers framework tooling categories: ESLint plugins, MCP servers, GitHub Actions, NPM packages, other tools'
    - 'Each candidate has a go/no-go decision with rationale'
    - 'Survey follows JSONL-as-source-of-truth pattern (JSONL canonical, MD generated)'
    - 'Timeboxed evaluation: quick scan all, deep dive only on adopt/maybe'
  artifacts:
    - path: 'CANON/RESOURCE_SURVEY.jsonl'
      provides: 'Machine-readable resource evaluation records'
      contains: 'decision'
    - path: 'CANON/RESOURCE_SURVEY.md'
      provides: 'Human-readable resource survey with decisions'
      contains: 'Category'
  key_links:
    - from: 'CANON/RESOURCE_SURVEY.jsonl'
      to: 'CANON/schemas/base-record.schema.ts'
      via: 'records follow base record format'
      pattern: 'id.*timestamp'
---

<objective>
Conduct the outside resource survey: scan candidate tools/plugins/services for framework tooling, evaluate each with go/no-go decisions, and document in JSONL + generated MD format.

Purpose: FOUND-05 requires an outside resource survey with documented decisions. This ensures the framework uses the best available tools and has a record of what was considered and why.
Output: RESOURCE_SURVEY.jsonl + RESOURCE_SURVEY.md
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@CLAUDE.md
@package.json
@eslint.config.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Quick Scan and Evaluate Resource Candidates</name>
  <files>CANON/RESOURCE_SURVEY.jsonl, CANON/RESOURCE_SURVEY.md</files>
  <action>
    Conduct a timeboxed outside resource survey for framework tooling. Per CONTEXT.md: "tiered -- quick scan all candidates (~30 min), deep dive only adopt/maybe shortlist."

    **Categories to evaluate** (5 categories per RESEARCH.md):

    1. **ESLint plugins**: Scan for useful ESLint plugins beyond what's already configured
       - Already have: custom eslint-plugin-framework (23 rules), @eslint/js
       - Candidates to evaluate: eslint-plugin-security, eslint-plugin-n (Node.js), eslint-plugin-jsonc, eslint-plugin-import-x
       - For each: does it add value beyond existing custom rules?

    2. **MCP servers**: Scan for Model Context Protocol servers useful for framework development
       - Candidates: filesystem MCP, git MCP, context7 (docs), sequential-thinking
       - For each: does it enhance Claude's capabilities for framework work?

    3. **GitHub Actions**: Scan for useful Actions beyond current CI setup
       - Already have: 9 workflows
       - Candidates: action-eslint, dependabot, codeql-analysis, actions/cache
       - For each: does it add value to CI pipeline?

    4. **NPM packages**: Scan for useful dev tools not currently installed
       - Already have: eslint, prettier, knip, madge, c8, tsx, vitest
       - Candidates: syncpack (monorepo version sync), depcheck, license-checker, npm-check-updates
       - For each: does it fill a gap in the current toolchain?

    5. **Other tools**: Any other tools that could enhance the framework
       - Candidates: commitlint, husky alternatives (lefthook), changesets, turbo

    **Record format** (JSONL):
    ```json
    {
      "id": "rs-{uuid}",
      "timestamp": "ISO-8601",
      "version": "1.0.0",
      "category": "eslint-plugins",
      "name": "eslint-plugin-security",
      "description": "Security-focused ESLint rules",
      "decision": "adopt|defer|reject",
      "rationale": "Why this decision",
      "priority": "high|medium|low",
      "evaluatedAt": "2026-03-02",
      "metadata": { "url": "...", "currentVersion": "..." }
    }
    ```

    **Generate RESOURCE_SURVEY.md** from the JSONL:
    - Summary: counts by decision (adopt/defer/reject) and category
    - Table per category: name, decision, rationale, priority
    - Adopt section: what to install/configure and when
    - Defer section: why deferred and what would trigger reconsideration

    Scope: framework tooling only (NOT consumer project recommendations -- those are Phase 9/v2).

  </action>
  <verify>
    - `CANON/RESOURCE_SURVEY.jsonl` exists with valid JSONL records
    - `CANON/RESOURCE_SURVEY.md` exists with summary and per-category tables
    - At least 15+ candidates evaluated across 5 categories
    - Every record has decision + rationale
    - `node -e "require('fs').readFileSync('CANON/RESOURCE_SURVEY.jsonl','utf8').trim().split('\\n').forEach(l=>JSON.parse(l))"` parses all lines
  </verify>
  <done>Resource survey JSONL created with 15+ candidates evaluated across 5 categories, each with go/no-go decision and rationale</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Review and approve resource survey decisions</decision>
  <context>
    Claude has evaluated 15+ candidate tools/plugins across 5 categories with adopt/defer/reject decisions.
    The user should review the decisions, especially any "adopt" items that would add new dependencies,
    and any "defer" items where the user might have specific preferences.
  </context>
  <options>
    <option id="approve-all">
      <name>Approve all decisions as-is</name>
      <pros>Fast, Claude's evaluation is based on codebase analysis</pros>
      <cons>May miss user preferences or context</cons>
    </option>
    <option id="modify">
      <name>Modify specific decisions</name>
      <pros>User input on tool choices</pros>
      <cons>Requires review time</cons>
    </option>
    <option id="add-candidates">
      <name>Add additional candidates to evaluate</name>
      <pros>More comprehensive survey</pros>
      <cons>Extends timeline</cons>
    </option>
  </options>
  <resume-signal>Select: approve-all, modify (with specifics), or add-candidates (with list)</resume-signal>
</task>

</tasks>

<verification>
- RESOURCE_SURVEY.jsonl exists with valid JSONL records
- RESOURCE_SURVEY.md is a readable summary matching the JSONL content
- All 5 categories represented
- Every candidate has a decision and rationale
- User has reviewed and approved decisions
</verification>

<success_criteria>

- FOUND-05 requirement satisfied: "Outside resource survey completed with go/no-go decisions"
- Survey dogfoods the JSONL-to-MD pattern from JSONL_STANDARD
- Decisions are documented with rationale for future reference
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-07-SUMMARY.md`
</output>
